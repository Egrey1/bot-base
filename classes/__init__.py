"""# Протокол  
Вот мой короткий протокол добавления новых полей в любой класс из [game_objects.py](d:/Visual%20Studio%20Code%20Projects/Python%20projects/discord%20projects/WOO%20bot/classes/objects/game_objects.py):

1. Определи, где поле живет.
   Если это реальная часть данных, сначала реши, в какой таблице и в какой колонке оно должно храниться.

2. Обнови схему БД и миграцию.
   Если поле новое, добавь его в `CREATE TABLE` внутри миграции или в логику пересоздания таблицы.
   Если проект уже использует старую БД, обязательно добавь автоперенос старых данных.

3. Загрузку делай в `__init__`.
   Каждый объект должен читать новое поле из `row[...]` сразу при создании экземпляра.

4. Протяни поле в `create()` и `edit()`.
   Если объект можно создать или изменить руками, новое поле должно уметь:
   - задаваться при создании
   - обновляться при редактировании
   - не ломать старые вызовы, если параметр не передан

5. Если это вложенный объект, не забудь обертки.
   Для `_UserBalance`, `_UserResources`, `_UserInventory` нужно отдельно проверить:
   - как поле загружается в `_reload()`
   - как оно сохраняется в `__setitem__()`
   - не теряется ли оно при `+=`, `-=` или повторной записи

6. Обнови `overloads.py`, если поле касается пользователя или роли.
   Если поле должно быть доступно через `user.get_*()` или `role.get_*()`, проверь, не нужно ли менять `autocreate_user()`, `create_role_information()` и похожие мосты.

7. Обнови `dependencies.py`.
   Минимум:
   - описание таблицы в `main_db`
   - новый атрибут в классе
   - типы в сигнатурах методов
   - заметки о поведении поля, если оно “служебное”, как `bank`

8. Проверь, не ломает ли поле UI и команды.
   Если объект выводится в embed, компоненте или команде, реши:
   - надо ли это поле показывать
   - должно ли оно влиять на логику
   - не изменился ли формат числа или строки

9. Если поле “пассивное”, явно сохраняй это правило.
   Как с `bank`: хранится, доступно в IDE, но нигде автоматически не участвует. Такие правила лучше соблюдать жестко, чтобы потом не получить случайную магию.

10. После добавления поля проверь три сценария.
   - создание нового объекта
   - чтение старого объекта из старой БД
   - редактирование поля и повторная загрузка объекта
"""
from .objects import *
from .objects import _UserBalance, _UserResources, _UserInventory
